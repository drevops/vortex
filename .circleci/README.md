CI configuration calls all DrevOps ahoy commands directly from `config.yml` file.

You may add custom scripts, which would run only in CI, to this directory and
reference them from `config.yml` file.
