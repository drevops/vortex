#!/usr/bin/env bash
##
# Deploy via pushing Docker image to Docker registry.
#

set -e
[ -n "${DREVOPS_DEBUG}" ] && set -x

echo "==> Started DOCKER deployment"

echo "Docker deployment is not yet implemented"

echo "==> Finished DOCKER deployment"
