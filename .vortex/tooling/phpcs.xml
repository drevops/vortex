<?xml version="1.0"?>
<ruleset name="custom">
    <description>Custom PHPCS standard.</description>

    <!-- Using Drupal coding standard as a base-fix. -->
    <rule ref="Drupal" />
    <rule ref="Generic.PHP.RequireStrictTypes" />
    <rule ref="DrevOps"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>
    <!-- Show progress of the run. -->
    <arg value="p"/>
    <arg name="colors"/>

    <file>check-no-exit.php</file>
    <file>playground</file>
    <file>src/vortex-deploy-artifact</file>
    <file>src/vortex-deploy-lagoon</file>
    <file>src/vortex-deploy-webhook</file>
    <file>src/vortex-deploy</file>
    <file>src/helpers.php</file>
    <file>src/vortex-login-container-registry</file>
    <file>src/vortex-notify-email</file>
    <file>src/vortex-notify-github</file>
    <file>src/vortex-notify-jira</file>
    <file>src/vortex-notify-newrelic</file>
    <file>src/vortex-notify-slack</file>
    <file>src/vortex-notify-webhook</file>
    <file>src/vortex-notify</file>
    <file>src/vortex-provision</file>
    <file>src/vortex-push-container-registry</file>
    <file>src/vortex-setup-ssh</file>
    <file>src</file>
    <file>tests</file>

    <!-- Allow delimiter spacing in inline comments in source files. -->
    <rule ref="Drupal.Commenting.InlineComment.SpacingAfter">
        <exclude-pattern>src\/.*</exclude-pattern>
    </rule>

    <!-- Allow long array lines in tests. -->
    <rule ref="Drupal.Arrays.Array.LongLineDeclaration">
        <exclude-pattern>*.Test\.php</exclude-pattern>
        <exclude-pattern>*.TestCase\.php</exclude-pattern>
        <exclude-pattern>*.test</exclude-pattern>
    </rule>

    <!-- Allow missing class description in tests. -->
    <rule ref="Drupal.Commenting.ClassComment.Missing">
        <exclude-pattern>*.Test\.php</exclude-pattern>
        <exclude-pattern>*.TestCase\.php</exclude-pattern>
        <exclude-pattern>*.test</exclude-pattern>
    </rule>

    <!-- Allow missing function names in tests. -->
    <rule ref="Drupal.Commenting.FunctionComment.Missing">
        <exclude-pattern>*.Test\.php</exclude-pattern>
        <exclude-pattern>*.TestCase\.php</exclude-pattern>
        <exclude-pattern>*.test</exclude-pattern>
    </rule>
</ruleset>
