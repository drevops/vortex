This directory contains Vortex and your custom scripts.

Add your custom per-project scripts into the `./scripts` directory. Scripts with a `provision-` prefix and a `.sh` extension run automatically during site provisioning.

Do not modify scripts shipped by Vortex.
