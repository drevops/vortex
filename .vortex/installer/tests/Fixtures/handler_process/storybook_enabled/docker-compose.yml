@@ -105,6 +105,8 @@
     depends_on:
       wait-for-dependencies:
         condition: service_completed_successfully
+    ports:
+      - '6006' # Storybook development server port in a container. Find port on host with `ahoy storybook` or `docker compose port cli 6006`.
     user: root
 
   nginx:
