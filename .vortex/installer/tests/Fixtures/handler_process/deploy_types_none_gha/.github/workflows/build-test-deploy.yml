@@ -528,197 +528,3 @@
         timeout-minutes: 120 # Cancel the action after 120 minutes, regardless of whether a connection has been established.
         with:
           detached: true
-
-  deploy:
-    runs-on: ubuntu-latest
-    needs: [build, lint]
-    if: ${{ !cancelled() && (inputs.deploy_target || (success() && github.event_name != 'schedule' && !startsWith(github.head_ref || github.ref_name, 'deps/') && (github.event_name == 'push' || !startsWith(github.head_ref, 'project/')))) }}
-
-    permissions:
-      contents: read
-      pull-requests: read
-
-    container:
-      # https://hub.docker.com/r/drevops/ci-runner
-      image: drevops/ci-runner:__VERSION__
-      env:
-        TZ: ${{ vars.TZ || 'UTC' }}
-        TERM: xterm-256color
-        VORTEX_SSH_DISABLE_STRICT_HOST_KEY_CHECKING: "1"
-        VORTEX_DEBUG: ${{ vars.VORTEX_DEBUG }}
-
-    steps:
-      - name: Preserve $HOME set in the container
-        run: echo HOME=/root >> "$GITHUB_ENV" # https://github.com/actions/runner/issues/863
-
-      - name: Resolve deploy target
-        if: ${{ inputs.deploy_target }}
-        env:
-          DEPLOY_TARGET: ${{ inputs.deploy_target }}
-          GH_TOKEN: ${{ github.token }}
-        run: |
-          {
-            echo "DEPLOY_BRANCH=${DEPLOY_TARGET}"
-            if echo "${DEPLOY_TARGET}" | grep -iq '^pr-'; then
-              echo "DEPLOY_PR_NUMBER=${DEPLOY_TARGET#*-}"
-              echo "DEPLOY_PR_HEAD_SHA=$(gh pr view "${DEPLOY_TARGET#*-}" --repo "${{ github.repository }}" --json headRefOid --jq '.headRefOid')"
-              echo "DEPLOY_BRANCH=$(gh pr view "${DEPLOY_TARGET#*-}" --repo "${{ github.repository }}" --json headRefName --jq '.headRefName')"
-            fi
-          } >> "$GITHUB_ENV"
-
-      - name: Checkout code
-        uses: actions/checkout@__HASH__ # __VERSION__
-        with:
-          # Fetch all history for git repository.
-          fetch-depth: 0
-          # Do not persist credentials after checkout
-          # to allow using the custom credentials to push to a remote repo.
-          persist-credentials: false
-          ref: ${{ env.DEPLOY_BRANCH || github.head_ref || github.ref_name }}
-
-      - name: Fix Git ownership permissions
-        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
-
-      - name: Load environment variables from .env
-        run: t=$(mktemp) && export -p >"${t}" && set -a && . ./.env && set +a && . "${t}" && env >> "$GITHUB_ENV"
-
-      # Artifact deployments do not work for manual deploys as the build job is skipped.
-      - name: Download exported codebase as an artifact
-        uses: actions/download-artifact@__HASH__ # __VERSION__
-        if: ${{ !inputs.deploy_target && contains(env.VORTEX_DEPLOY_TYPES, 'artifact') }}
-        with:
-          name: code-artifact
-          path: "/tmp/artifacts"
-
-      - name: Unpack downloaded exported codebase
-        if: ${{ !inputs.deploy_target && contains(env.VORTEX_DEPLOY_TYPES, 'artifact') }}
-        run: |
-          mkdir -p /tmp/workspace
-          tar -xpf /tmp/artifacts/code.tar -C /tmp/workspace
-
-      - name: Add SSH private key to the runner
-        if: ${{ env.VORTEX_DEPLOY_SSH_PRIVATE_KEY != '' }}
-        uses: shimataro/ssh-key-action@__HASH__ # __VERSION__
-        with:
-          key: ${{ secrets.VORTEX_DEPLOY_SSH_PRIVATE_KEY }}
-          known_hosts: ${{ vars.VORTEX_DEPLOY_SSH_KNOWN_HOSTS || 'unnecessary' }}
-        env:
-          VORTEX_DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.VORTEX_DEPLOY_SSH_PRIVATE_KEY }}
-
-      - name: Install Vortex tooling
-        run: ./scripts/vortex-tooling.sh
-
-      # The deployment and its summary report the same values, so they are
-      # resolved into the job environment rather than a single step's env block.
-      - name: Resolve deployment inputs
-        env:
-          GITHUB_EVENT_NUMBER: ${{ github.event.number }}
-          INPUTS_OVERRIDE_DB: ${{ inputs.override_db }}
-        run: |
-          set -euo pipefail
-
-          mode=branch
-          case "${GITHUB_REF}" in refs/tags/*) mode=tag ;; esac
-
-          action=
-          if [ "${INPUTS_OVERRIDE_DB}" = "true" ]; then
-            action=deploy_override_db
-          fi
-
-          {
-            echo "VORTEX_DEPLOY_MODE=${mode}"
-            echo "VORTEX_DEPLOY_ACTION=${action}"
-            echo "VORTEX_DEPLOY_BRANCH=${DEPLOY_BRANCH:-${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}}"
-            echo "VORTEX_DEPLOY_PR=${DEPLOY_PR_NUMBER:-${GITHUB_EVENT_NUMBER}}"
-          } >> "$GITHUB_ENV"
-
-      - name: Deploy
-        id: deploy
-        run: |
-          set -euo pipefail
-          mkdir -p .logs
-          ./vendor/bin/vortex-deploy 2>&1 | tee .logs/deploy.log
-        env:
-          VORTEX_DEPLOY_PR_HEAD: ${{ env.DEPLOY_PR_HEAD_SHA || github.event.pull_request.head.sha }}
-          VORTEX_DEPLOY_PR_LABELS: ${{ join(github.event.pull_request.labels.*.name, ',') }}
-          VORTEX_DEPLOY_ARTIFACT_SRC: /tmp/workspace/code
-          VORTEX_DEPLOY_ARTIFACT_ROOT: ${{ github.workspace }}
-          VORTEX_DEPLOY_ARTIFACT_GIT_REMOTE: ${{ vars.VORTEX_DEPLOY_ARTIFACT_GIT_REMOTE }}
-          VORTEX_DEPLOY_ARTIFACT_GIT_USER_EMAIL: ${{ vars.VORTEX_DEPLOY_ARTIFACT_GIT_USER_EMAIL }}
-          VORTEX_DEPLOY_ARTIFACT_GIT_USER_NAME: ${{ vars.VORTEX_DEPLOY_ARTIFACT_GIT_USER_NAME }}
-          VORTEX_DEPLOY_WEBHOOK_URL: ${{ vars.VORTEX_DEPLOY_WEBHOOK_URL }}
-          VORTEX_DEPLOY_SKIP: ${{ vars.VORTEX_DEPLOY_SKIP }}
-          VORTEX_DEPLOY_ALLOW_SKIP: ${{ vars.VORTEX_DEPLOY_ALLOW_SKIP }}
-          VORTEX_DEPLOY_SKIP_PRS: ${{ vars.VORTEX_DEPLOY_SKIP_PRS }}
-          VORTEX_DEPLOY_SKIP_BRANCHES: ${{ vars.VORTEX_DEPLOY_SKIP_BRANCHES }}
-          VORTEX_DEPLOY_ALLOW_LABEL: ${{ vars.VORTEX_DEPLOY_ALLOW_LABEL }}
-        timeout-minutes: 30
-
-      # The skip flags and the label gate exit successfully, so a green deploy
-      # job does not mean that anything was deployed.
-      - name: Write deployment summary
-        if: ${{ !cancelled() && vars.VORTEX_CI_DEPLOY_SUMMARY_SKIP != '1' }}
-        env:
-          STEPS_DEPLOY_OUTCOME: ${{ steps.deploy.outcome }}
-        run: |
-          set -euo pipefail
-
-          log=.logs/deploy.log
-          log_lines=200
-
-          status=Succeeded
-          if [ "${STEPS_DEPLOY_OUTCOME}" != "success" ]; then
-            status=Failed
-          fi
-
-          # A skip exits successfully, so the marker in the log is the only
-          # signal that nothing was deployed.
-          reason=
-          if [ -s "${log}" ] && grep -q "Skipped deployment" "${log}"; then
-            status=Skipped
-            reason="$(grep -B 1 "Skipped deployment" "${log}" | head -n 1 | sed -e $'s/\033\\[[0-9;]*m//g' -e 's/^ *//' -e 's/ *$//' || true)"
-          fi
-
-          {
-            echo "### Deployment"
-            echo
-            if [ -n "${reason}" ]; then
-              echo "**${status}** - ${reason}"
-            else
-              echo "**${status}**"
-            fi
-            echo
-            echo "| Setting | Value |"
-            echo "| --- | --- |"
-            echo "| Types | \`${VORTEX_DEPLOY_TYPES:-}\` |"
-            echo "| Mode | \`${VORTEX_DEPLOY_MODE:-}\` |"
-            echo "| Branch | \`${VORTEX_DEPLOY_BRANCH:-}\` |"
-            if [ -n "${VORTEX_DEPLOY_PR:-}" ]; then
-              echo "| Pull request | \`${VORTEX_DEPLOY_PR}\` |"
-            fi
-            if [ -n "${VORTEX_DEPLOY_ACTION:-}" ]; then
-              echo "| Action | \`${VORTEX_DEPLOY_ACTION}\` |"
-            fi
-          } >> "$GITHUB_STEP_SUMMARY"
-
-          # The job summary has a 1MiB limit and the artifact builder runs
-          # verbosely, so only the tail of the log is embedded, without colors.
-          if [ -s "${log}" ]; then
-            total="$(wc -l <"${log}" | tr -d ' ')"
-
-            {
-              echo
-              echo "<details>"
-              echo "<summary>Deployment log</summary>"
-              echo
-              if [ "${total}" -gt "${log_lines}" ]; then
-                echo "Showing the last ${log_lines} of ${total} lines."
-                echo
-              fi
-              echo '```'
-              tail -n "${log_lines}" "${log}" | sed -e $'s/\033\\[[0-9;]*m//g'
-              echo '```'
-              echo
-              echo "</details>"
-            } >> "$GITHUB_STEP_SUMMARY"
-          fi
