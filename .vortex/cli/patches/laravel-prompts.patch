diff -ruN a/src/AutoCompletePrompt.php b/src/AutoCompletePrompt.php
--- a/src/AutoCompletePrompt.php
+++ b/src/AutoCompletePrompt.php
@@ -39,6 +39,7 @@
         public mixed $validate = null,
         public string $hint = '',
         public ?Closure $transform = null,
+        public ?string $description = null,
     ) {
         $this->options = $options instanceof Collection ? $options->all() : $options;
 
diff -ruN a/src/ConfirmPrompt.php b/src/ConfirmPrompt.php
--- a/src/ConfirmPrompt.php
+++ b/src/ConfirmPrompt.php
@@ -23,6 +23,7 @@
         public mixed $validate = null,
         public string $hint = '',
         public ?Closure $transform = null,
+        public ?string $description = null,
     ) {
         $this->confirmed = $default;
 
diff -ruN a/src/FormBuilder.php b/src/FormBuilder.php
--- a/src/FormBuilder.php
+++ b/src/FormBuilder.php
@@ -88,7 +88,7 @@
     /**
      * Prompt the user for text input.
      */
-    public function text(string $label, string $placeholder = '', string $default = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null): self
+    public function text(string $label, string $placeholder = '', string $default = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(text(...), get_defined_vars());
     }
@@ -96,7 +96,7 @@
     /**
      * Prompt the user for multiline text input.
      */
-    public function textarea(string $label, string $placeholder = '', string $default = '', bool|string $required = false, ?Closure $validate = null, string $hint = '', int $rows = 5, ?string $name = null, ?Closure $transform = null): self
+    public function textarea(string $label, string $placeholder = '', string $default = '', bool|string $required = false, ?Closure $validate = null, string $hint = '', int $rows = 5, ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(textarea(...), get_defined_vars());
     }
@@ -104,7 +104,7 @@
     /**
      * Prompt the user for input, hiding the value.
      */
-    public function password(string $label, string $placeholder = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null): self
+    public function password(string $label, string $placeholder = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(password(...), get_defined_vars());
     }
@@ -115,7 +115,7 @@
      * @param  array<int|string, string>|Collection<int|string, string>  $options
      * @param  true|string  $required
      */
-    public function select(string $label, array|Collection $options, int|string|null $default = null, int $scroll = 5, mixed $validate = null, string $hint = '', bool|string $required = true, ?string $name = null, ?Closure $transform = null): self
+    public function select(string $label, array|Collection $options, int|string|null $default = null, int $scroll = 5, mixed $validate = null, string $hint = '', bool|string $required = true, ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(select(...), get_defined_vars());
     }
@@ -126,7 +126,7 @@
      * @param  array<int|string, string>|Collection<int|string, string>  $options
      * @param  array<int|string>|Collection<int, int|string>  $default
      */
-    public function multiselect(string $label, array|Collection $options, array|Collection $default = [], int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = 'Use the space bar to select options.', ?string $name = null, ?Closure $transform = null): self
+    public function multiselect(string $label, array|Collection $options, array|Collection $default = [], int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = 'Use the space bar to select options.', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(multiselect(...), get_defined_vars());
     }
@@ -134,7 +134,7 @@
     /**
      * Prompt the user to confirm an action.
      */
-    public function confirm(string $label, bool $default = true, string $yes = 'Yes', string $no = 'No', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null): self
+    public function confirm(string $label, bool $default = true, string $yes = 'Yes', string $no = 'No', bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(confirm(...), get_defined_vars());
     }
@@ -152,7 +152,7 @@
      *
      * @param  array<string>|Collection<int, string>|Closure(string): array<string>  $options
      */
-    public function suggest(string $label, array|Collection|Closure $options, string $placeholder = '', string $default = '', int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null): self
+    public function suggest(string $label, array|Collection|Closure $options, string $placeholder = '', string $default = '', int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = '', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(suggest(...), get_defined_vars());
     }
@@ -163,7 +163,7 @@
      * @param  Closure(string): array<int|string, string>  $options
      * @param  true|string  $required
      */
-    public function search(string $label, Closure $options, string $placeholder = '', int $scroll = 5, mixed $validate = null, string $hint = '', bool|string $required = true, ?string $name = null, ?Closure $transform = null): self
+    public function search(string $label, Closure $options, string $placeholder = '', int $scroll = 5, mixed $validate = null, string $hint = '', bool|string $required = true, ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(search(...), get_defined_vars());
     }
@@ -173,7 +173,7 @@
      *
      * @param  Closure(string): array<int|string, string>  $options
      */
-    public function multisearch(string $label, Closure $options, string $placeholder = '', int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = 'Use the space bar to select options.', ?string $name = null, ?Closure $transform = null): self
+    public function multisearch(string $label, Closure $options, string $placeholder = '', int $scroll = 5, bool|string $required = false, mixed $validate = null, string $hint = 'Use the space bar to select options.', ?string $name = null, ?Closure $transform = null, ?string $description = null): self
     {
         return $this->runPrompt(multisearch(...), get_defined_vars());
     }
diff -ruN a/src/MultiSearchPrompt.php b/src/MultiSearchPrompt.php
--- a/src/MultiSearchPrompt.php
+++ b/src/MultiSearchPrompt.php
@@ -46,6 +46,7 @@
         public string $hint = '',
         public ?Closure $transform = null,
         public string|Closure $info = '',
+        public ?string $description = null,
     ) {
         $this->trackTypedValue(submit: false, ignore: fn ($key) => Key::oneOf([Key::SPACE, Key::HOME, Key::END, Key::CTRL_A, Key::CTRL_E], $key) && $this->highlighted !== null);
 
diff -ruN a/src/MultiSelectPrompt.php b/src/MultiSelectPrompt.php
--- a/src/MultiSelectPrompt.php
+++ b/src/MultiSelectPrompt.php
@@ -47,6 +47,7 @@
         public string $hint = '',
         public ?Closure $transform = null,
         public string|Closure $info = '',
+        public ?string $description = null,
     ) {
         $this->options = $options instanceof Collection ? $options->all() : $options;
         $this->default = $default instanceof Collection ? $default->all() : $default;
diff -ruN a/src/NumberPrompt.php b/src/NumberPrompt.php
--- a/src/NumberPrompt.php
+++ b/src/NumberPrompt.php
@@ -23,6 +23,7 @@
         public ?int $min = null,
         public ?int $max = null,
         public ?int $step = null,
+        public ?string $description = null,
     ) {
         $this->trackTypedValue($default);
 
diff -ruN a/src/PasswordPrompt.php b/src/PasswordPrompt.php
--- a/src/PasswordPrompt.php
+++ b/src/PasswordPrompt.php
@@ -18,6 +18,7 @@
         public mixed $validate = null,
         public string $hint = '',
         public ?Closure $transform = null,
+        public ?string $description = null,
     ) {
         $this->trackTypedValue();
     }
diff -ruN a/src/Prompt.php b/src/Prompt.php
--- a/src/Prompt.php
+++ b/src/Prompt.php
@@ -249,7 +249,7 @@
     /**
      * Set the custom validation callback.
      */
-    public static function validateUsing(Closure $callback): void
+    public static function validateUsing(?Closure $callback): void
     {
         static::$validateUsing = $callback;
     }
@@ -403,8 +403,8 @@
         }
 
         $error = match (true) {
+            isset(static::$validateUsing) => (static::$validateUsing)($this, $value),
             is_callable($this->validate) => ($this->validate)($value),
-            isset(static::$validateUsing) => (static::$validateUsing)($this),
             default => throw new RuntimeException('The validation logic is missing.'),
         };
 
diff -ruN a/src/SearchPrompt.php b/src/SearchPrompt.php
--- a/src/SearchPrompt.php
+++ b/src/SearchPrompt.php
@@ -34,6 +34,7 @@
         public bool|string $required = true,
         public ?Closure $transform = null,
         public string|Closure $info = '',
+        public ?string $description = null,
     ) {
         if ($this->required === false) {
             throw new InvalidArgumentException('Argument [required] must be true or a string.');
diff -ruN a/src/SelectPrompt.php b/src/SelectPrompt.php
--- a/src/SelectPrompt.php
+++ b/src/SelectPrompt.php
@@ -33,6 +33,7 @@
         public bool|string $required = true,
         public ?Closure $transform = null,
         public string|Closure $info = '',
+        public ?string $description = null,
     ) {
         if ($this->required === false) {
             throw new InvalidArgumentException('Argument [required] must be true or a string.');
diff -ruN a/src/SuggestPrompt.php b/src/SuggestPrompt.php
--- a/src/SuggestPrompt.php
+++ b/src/SuggestPrompt.php
@@ -42,6 +42,7 @@
         public string $hint = '',
         public ?Closure $transform = null,
         public string|Closure $info = '',
+        public ?string $description = null,
     ) {
         $this->options = $options instanceof Collection ? $options->all() : $options;
 
diff -ruN a/src/TextPrompt.php b/src/TextPrompt.php
--- a/src/TextPrompt.php
+++ b/src/TextPrompt.php
@@ -19,6 +19,7 @@
         public mixed $validate = null,
         public string $hint = '',
         public ?Closure $transform = null,
+        public ?string $description = null,
     ) {
         $this->trackTypedValue($default);
     }
diff -ruN a/src/TextareaPrompt.php b/src/TextareaPrompt.php
--- a/src/TextareaPrompt.php
+++ b/src/TextareaPrompt.php
@@ -31,6 +31,7 @@
         public string $hint = '',
         int $rows = 5,
         public ?Closure $transform = null,
+        public ?string $description = null,
     ) {
         $this->scroll = $rows;
 
diff -ruN a/src/Themes/Default/AutoCompletePromptRenderer.php b/src/Themes/Default/AutoCompletePromptRenderer.php
--- a/src/Themes/Default/AutoCompletePromptRenderer.php
+++ b/src/Themes/Default/AutoCompletePromptRenderer.php
@@ -3,10 +3,12 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\AutoCompletePrompt;
+use Laravel\Prompts\Prompt;
 
 class AutoCompletePromptRenderer extends Renderer
 {
     use Concerns\DrawsBoxes;
+    use Concerns\RendersDescription;
 
     /**
      * Render the text prompt.
@@ -14,18 +16,21 @@
     public function __invoke(AutoCompletePrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->value(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -33,7 +38,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth) : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -41,7 +47,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth) : '',
                 )
                 ->when(
                     $prompt->hint,
@@ -49,5 +56,23 @@
                     fn () => $this->newLine() // Space for errors
                 )
         };
+    }
+
+    /**
+     * Calculate the description width based on input content.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof AutoCompletePrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $inputWidth = max(
+            mb_strwidth($this->stripEscapeSequences($prompt->value() ?: $prompt->placeholder)),
+            40 // minimum reasonable input width
+        );
+
+        return max($this->minWidth, max($titleWidth, min($inputWidth, $maxWidth)));
     }
 }
diff -ruN a/src/Themes/Default/Concerns/RendersDescription.php b/src/Themes/Default/Concerns/RendersDescription.php
--- a/src/Themes/Default/Concerns/RendersDescription.php
+++ b/src/Themes/Default/Concerns/RendersDescription.php
@@ -0,0 +1,33 @@
+<?php
+
+namespace Laravel\Prompts\Themes\Default\Concerns;
+
+use Closure;
+use Laravel\Prompts\Prompt;
+
+trait RendersDescription
+{
+    /**
+     * Render a description for the given prompt, wrapping it to fit within the calculated box width.
+     */
+    protected function renderDescription(Prompt $prompt, int $maxWidth, ?Closure $calculateContentWidth = null): string
+    {
+        if (! property_exists($prompt, 'description') || ! $prompt->description || trim($prompt->description) === '') {
+            return '';
+        }
+
+        $targetWidth = $calculateContentWidth ? $calculateContentWidth() : $this->calculateDescriptionWidth($prompt, $maxWidth);
+
+        return $this->mbWordwrap($prompt->description, $targetWidth, PHP_EOL);
+    }
+
+    /**
+     * Calculate the default description width based on title and minWidth.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+
+        return max($this->minWidth, min($titleWidth, $maxWidth));
+    }
+}
diff -ruN a/src/Themes/Default/ConfirmPromptRenderer.php b/src/Themes/Default/ConfirmPromptRenderer.php
--- a/src/Themes/Default/ConfirmPromptRenderer.php
+++ b/src/Themes/Default/ConfirmPromptRenderer.php
@@ -3,27 +3,34 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\ConfirmPrompt;
+use Laravel\Prompts\Prompt;
 
 class ConfirmPromptRenderer extends Renderer
 {
     use Concerns\DrawsBoxes;
+    use Concerns\RendersDescription;
 
     /**
      * Render the confirm prompt.
      */
     public function __invoke(ConfirmPrompt $prompt): string
     {
+        $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
+
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->label(), $prompt->terminal()->cols() - 6)
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->label(), $prompt->terminal()->cols() - 6),
+                    $hasDescription ? $this->truncate($prompt->label(), $prompt->terminal()->cols() - 6) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'red'
                 )
                 ->error($prompt->cancelMessage),
@@ -31,7 +38,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -39,7 +47,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                 )
                 ->when(
                     $prompt->hint,
@@ -47,6 +56,21 @@
                     fn () => $this->newLine() // Space for errors
                 ),
         };
+    }
+
+    /**
+     * Calculate the description width based on buttons.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof ConfirmPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $buttonsWidth = mb_strwidth($prompt->yes.' / '.$prompt->no) + 10; // padding for buttons
+
+        return max($this->minWidth, max($titleWidth, min($buttonsWidth, $maxWidth)));
     }
 
     /**
diff -ruN a/src/Themes/Default/MultiSearchPromptRenderer.php b/src/Themes/Default/MultiSearchPromptRenderer.php
--- a/src/Themes/Default/MultiSearchPromptRenderer.php
+++ b/src/Themes/Default/MultiSearchPromptRenderer.php
@@ -3,12 +3,14 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\MultiSearchPrompt;
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
 class MultiSearchPromptRenderer extends Renderer implements Scrolling
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the suggest prompt.
@@ -16,18 +18,21 @@
     public function __invoke(MultiSearchPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->renderSelectedOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderSelectedOptions($prompt),
+                    $hasDescription ? $this->renderSelectedOptions($prompt) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -35,8 +40,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $prompt->valueWithCursor($maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     color: 'yellow',
                     info: $this->getInfoText($prompt),
                 )
@@ -45,8 +50,8 @@
             'searching' => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->valueWithCursorAndSearchIcon($prompt, $maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->valueWithCursorAndSearchIcon($prompt, $maxWidth),
+                    $hasDescription ? $this->valueWithCursorAndSearchIcon($prompt, $maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     info: $this->getInfoText($prompt),
                 )
                 ->hint($prompt->hint),
@@ -54,8 +59,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->valueWithCursor($maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     info: $this->getInfoText($prompt),
                 )
                 ->when(
@@ -168,6 +173,21 @@
         $parts = array_filter([$prompt->infoText(), $selected]);
 
         return implode(' · ', $parts);
+    }
+
+    /**
+     * Calculate the width for description text.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof MultiSearchPrompt) {
+            return $maxWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $optionsWidth = $this->longest($prompt->matches());
+
+        return max($this->minWidth, max($titleWidth, $optionsWidth));
     }
 
     /**
diff -ruN a/src/Themes/Default/MultiSelectPromptRenderer.php b/src/Themes/Default/MultiSelectPromptRenderer.php
--- a/src/Themes/Default/MultiSelectPromptRenderer.php
+++ b/src/Themes/Default/MultiSelectPromptRenderer.php
@@ -3,29 +3,36 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\MultiSelectPrompt;
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
 class MultiSelectPromptRenderer extends Renderer implements Scrolling
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the multiselect prompt.
      */
     public function __invoke(MultiSelectPrompt $prompt): string
     {
+        $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
+
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->renderSelectedOptions($prompt)
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderSelectedOptions($prompt),
+                    $hasDescription ? $this->renderSelectedOptions($prompt) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -33,7 +40,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'yellow',
                     info: count($prompt->options) > $prompt->scroll ? (count($prompt->value()).' selected') : '',
                 )
@@ -42,7 +50,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     info: $this->getInfoText($prompt),
                 )
                 ->when(
@@ -51,6 +60,21 @@
                     fn () => $this->newLine() // Space for errors
                 ),
         };
+    }
+
+    /**
+     * Calculate the description width based on options.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof MultiSelectPrompt) {
+            return $this->minWidth;
+        }
+
+        $optionsWidth = min($this->longest($prompt->options, padding: 6), $prompt->terminal()->cols() - 6);
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+
+        return max($this->minWidth, max($titleWidth, $optionsWidth));
     }
 
     /**
diff -ruN a/src/Themes/Default/NumberPromptRenderer.php b/src/Themes/Default/NumberPromptRenderer.php
--- a/src/Themes/Default/NumberPromptRenderer.php
+++ b/src/Themes/Default/NumberPromptRenderer.php
@@ -3,10 +3,12 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\NumberPrompt;
+use Laravel\Prompts\Prompt;
 
 class NumberPromptRenderer extends Renderer
 {
     use Concerns\DrawsBoxes;
+    use Concerns\RendersDescription;
 
     protected string $upArrow = '▲';
 
@@ -18,18 +20,21 @@
     public function __invoke(NumberPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate((string) $prompt->value(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate((string) $prompt->value(), $maxWidth),
+                    $hasDescription ? $this->truncate((string) $prompt->value(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->strikethrough($this->dim($this->truncate((string) $prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate((string) $prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate((string) $prompt->value() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error('Cancelled.'),
@@ -37,7 +42,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth), 'yellow'),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth), 'yellow'),
+                    $hasDescription ? $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth), 'yellow') : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -45,7 +51,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth)),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth)),
+                    $hasDescription ? $this->withArrows($prompt, $prompt->valueWithCursor($maxWidth)) : '',
                 )
                 ->when(
                     $prompt->hint,
@@ -53,6 +60,24 @@
                     fn () => $this->newLine() // Space for errors
                 )
         };
+    }
+
+    /**
+     * Calculate the description width based on input content.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof NumberPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $inputWidth = max(
+            mb_strwidth($this->stripEscapeSequences((string) $prompt->value() ?: $prompt->placeholder)),
+            40 // minimum reasonable input width
+        );
+
+        return max($this->minWidth, max($titleWidth, min($inputWidth, $maxWidth)));
     }
 
     protected function withArrows(NumberPrompt $prompt, int|string $value, ?string $color = null): string
diff -ruN a/src/Themes/Default/PasswordPromptRenderer.php b/src/Themes/Default/PasswordPromptRenderer.php
--- a/src/Themes/Default/PasswordPromptRenderer.php
+++ b/src/Themes/Default/PasswordPromptRenderer.php
@@ -3,10 +3,12 @@
 namespace Laravel\Prompts\Themes\Default;
 
 use Laravel\Prompts\PasswordPrompt;
+use Laravel\Prompts\Prompt;
 
 class PasswordPromptRenderer extends Renderer
 {
     use Concerns\DrawsBoxes;
+    use Concerns\RendersDescription;
 
     /**
      * Render the password prompt.
@@ -14,18 +16,21 @@
     public function __invoke(PasswordPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($prompt->label),
-                    $this->truncate($prompt->masked(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->masked(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->masked(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->strikethrough($this->dim($this->truncate($prompt->masked() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->masked() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->masked() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -33,7 +38,8 @@
             'error' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->maskedWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->maskedWithCursor($maxWidth),
+                    $hasDescription ? $prompt->maskedWithCursor($maxWidth) : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -41,7 +47,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->maskedWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->maskedWithCursor($maxWidth),
+                    $hasDescription ? $prompt->maskedWithCursor($maxWidth) : '',
                 )
                 ->when(
                     $prompt->hint,
@@ -49,5 +56,23 @@
                     fn () => $this->newLine() // Space for errors
                 ),
         };
+    }
+
+    /**
+     * Calculate the description width based on password input content.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof PasswordPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $inputWidth = max(
+            mb_strwidth($this->stripEscapeSequences($prompt->masked() ?: $prompt->placeholder)),
+            40 // minimum reasonable input width
+        );
+
+        return max($this->minWidth, max($titleWidth, min($inputWidth, $maxWidth)));
     }
 }
diff -ruN a/src/Themes/Default/SearchPromptRenderer.php b/src/Themes/Default/SearchPromptRenderer.php
--- a/src/Themes/Default/SearchPromptRenderer.php
+++ b/src/Themes/Default/SearchPromptRenderer.php
@@ -2,6 +2,7 @@
 
 namespace Laravel\Prompts\Themes\Default;
 
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\SearchPrompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
@@ -9,6 +10,7 @@
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the suggest prompt.
@@ -16,18 +18,21 @@
     public function __invoke(SearchPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->label(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->label(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->label(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->searchValue() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -35,8 +40,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $prompt->valueWithCursor($maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -44,8 +49,8 @@
             'searching' => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->valueWithCursorAndSearchIcon($prompt, $maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->valueWithCursorAndSearchIcon($prompt, $maxWidth),
+                    $hasDescription ? $this->valueWithCursorAndSearchIcon($prompt, $maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     info: $prompt->infoText(),
                 )
                 ->hint($prompt->hint),
@@ -53,8 +58,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->valueWithCursor($maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     info: $prompt->infoText(),
                 )
                 ->when(
@@ -123,6 +128,21 @@
             count($prompt->matches()),
             min($this->longest($prompt->matches(), padding: 4), $prompt->terminal()->cols() - 6)
         ));
+    }
+
+    /**
+     * Calculate the width for description text.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof SearchPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $optionsWidth = $this->longest($prompt->matches());
+
+        return max($this->minWidth, max($titleWidth, $optionsWidth));
     }
 
     /**
diff -ruN a/src/Themes/Default/SelectPromptRenderer.php b/src/Themes/Default/SelectPromptRenderer.php
--- a/src/Themes/Default/SelectPromptRenderer.php
+++ b/src/Themes/Default/SelectPromptRenderer.php
@@ -2,6 +2,7 @@
 
 namespace Laravel\Prompts\Themes\Default;
 
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\SelectPrompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
@@ -9,6 +10,7 @@
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the select prompt.
@@ -17,17 +19,21 @@
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
 
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
+
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->label(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->label(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->label(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -35,7 +41,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -43,7 +50,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderOptions($prompt) : '',
                     info: $prompt->infoText(),
                 )
                 ->when(
@@ -52,6 +60,21 @@
                     fn () => $this->newLine() // Space for errors
                 ),
         };
+    }
+
+    /**
+     * Calculate the description width based on options.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof SelectPrompt) {
+            return $maxWidth;
+        }
+
+        $optionsWidth = min($this->longest($prompt->options, padding: 6), $prompt->terminal()->cols() - 6);
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+
+        return max($this->minWidth, max($titleWidth, $optionsWidth));
     }
 
     /**
diff -ruN a/src/Themes/Default/SuggestPromptRenderer.php b/src/Themes/Default/SuggestPromptRenderer.php
--- a/src/Themes/Default/SuggestPromptRenderer.php
+++ b/src/Themes/Default/SuggestPromptRenderer.php
@@ -2,6 +2,7 @@
 
 namespace Laravel\Prompts\Themes\Default;
 
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\SuggestPrompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
@@ -9,6 +10,7 @@
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the suggest prompt.
@@ -16,18 +18,21 @@
     public function __invoke(SuggestPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->value(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -35,8 +40,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->valueWithCursorAndArrow($prompt, $maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->valueWithCursorAndArrow($prompt, $maxWidth),
+                    $hasDescription ? $this->valueWithCursorAndArrow($prompt, $maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -44,8 +49,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->valueWithCursorAndArrow($prompt, $maxWidth),
-                    $this->renderOptions($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->valueWithCursorAndArrow($prompt, $maxWidth),
+                    $hasDescription ? $this->valueWithCursorAndArrow($prompt, $maxWidth).PHP_EOL.$this->renderOptions($prompt) : $this->renderOptions($prompt),
                     info: $prompt->infoText(),
                 )
                 ->when(
@@ -112,6 +117,25 @@
             min($this->longest($prompt->matches(), padding: 4), $prompt->terminal()->cols() - 6),
             $prompt->state === 'cancel' ? 'dim' : 'cyan'
         ));
+    }
+
+    /**
+     * Calculate the width for description text.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof SuggestPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $inputWidth = max(
+            mb_strwidth($prompt->placeholder),
+            mb_strwidth($prompt->default),
+            $this->longest($prompt->matches())
+        );
+
+        return max($this->minWidth, max($titleWidth, min($inputWidth + 4, $maxWidth)));
     }
 
     /**
diff -ruN a/src/Themes/Default/TextPromptRenderer.php b/src/Themes/Default/TextPromptRenderer.php
--- a/src/Themes/Default/TextPromptRenderer.php
+++ b/src/Themes/Default/TextPromptRenderer.php
@@ -2,11 +2,13 @@
 
 namespace Laravel\Prompts\Themes\Default;
 
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\TextPrompt;
 
 class TextPromptRenderer extends Renderer
 {
     use Concerns\DrawsBoxes;
+    use Concerns\RendersDescription;
 
     /**
      * Render the text prompt.
@@ -14,18 +16,21 @@
     public function __invoke(TextPrompt $prompt): string
     {
         $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->truncate($prompt->value(), $maxWidth),
+                    $hasDescription ? $this->truncate($prompt->value(), $maxWidth) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))),
+                    $hasDescription ? $this->strikethrough($this->dim($this->truncate($prompt->value() ?: $prompt->placeholder, $maxWidth))) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -33,7 +38,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->terminal()->cols() - 6),
-                    $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth) : '',
                     color: 'yellow',
                 )
                 ->warning($this->truncate($prompt->error, $prompt->terminal()->cols() - 5)),
@@ -41,7 +47,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->terminal()->cols() - 6)),
-                    $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $prompt->valueWithCursor($maxWidth),
+                    $hasDescription ? $prompt->valueWithCursor($maxWidth) : '',
                 )
                 ->when(
                     $prompt->hint,
@@ -49,5 +56,23 @@
                     fn () => $this->newLine() // Space for errors
                 )
         };
+    }
+
+    /**
+     * Calculate the description width based on input content.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof TextPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $inputWidth = max(
+            mb_strwidth($this->stripEscapeSequences($prompt->value() ?: $prompt->placeholder)),
+            40 // minimum reasonable input width
+        );
+
+        return max($this->minWidth, max($titleWidth, min($inputWidth, $maxWidth)));
     }
 }
diff -ruN a/src/Themes/Default/TextareaPromptRenderer.php b/src/Themes/Default/TextareaPromptRenderer.php
--- a/src/Themes/Default/TextareaPromptRenderer.php
+++ b/src/Themes/Default/TextareaPromptRenderer.php
@@ -2,6 +2,7 @@
 
 namespace Laravel\Prompts\Themes\Default;
 
+use Laravel\Prompts\Prompt;
 use Laravel\Prompts\TextareaPrompt;
 use Laravel\Prompts\Themes\Contracts\Scrolling;
 
@@ -9,6 +10,7 @@
 {
     use Concerns\DrawsBoxes;
     use Concerns\DrawsScrollbars;
+    use Concerns\RendersDescription;
 
     /**
      * Render the textarea prompt.
@@ -16,18 +18,22 @@
     public function __invoke(TextareaPrompt $prompt): string
     {
         $prompt->width = $prompt->terminal()->cols() - 8;
+        $maxWidth = $prompt->terminal()->cols() - 6;
+        $hasDescription = $prompt->description && trim($prompt->description) !== '';
 
         return match ($prompt->state) {
             'submit' => $this
                 ->box(
                     $this->dim($this->truncate($prompt->label, $prompt->width)),
-                    implode(PHP_EOL, $prompt->lines()),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : implode(PHP_EOL, $prompt->lines()),
+                    $hasDescription ? implode(PHP_EOL, $prompt->lines()) : '',
                 ),
 
             'cancel' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->width),
-                    implode(PHP_EOL, array_map(fn ($line) => $this->strikethrough($this->dim($line)), $prompt->lines())),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : implode(PHP_EOL, array_map(fn ($line) => $this->strikethrough($this->dim($line)), $prompt->lines())),
+                    $hasDescription ? implode(PHP_EOL, array_map(fn ($line) => $this->strikethrough($this->dim($line)), $prompt->lines())) : '',
                     color: 'red',
                 )
                 ->error($prompt->cancelMessage),
@@ -35,7 +41,8 @@
             'error' => $this
                 ->box(
                     $this->truncate($prompt->label, $prompt->width),
-                    $this->renderText($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderText($prompt),
+                    $hasDescription ? $this->renderText($prompt) : '',
                     color: 'yellow',
                     info: 'Ctrl+D to submit'
                 )
@@ -44,7 +51,8 @@
             default => $this
                 ->box(
                     $this->cyan($this->truncate($prompt->label, $prompt->width)),
-                    $this->renderText($prompt),
+                    $hasDescription ? $this->renderDescription($prompt, $maxWidth, fn () => $this->calculateDescriptionWidth($prompt, $maxWidth)) : $this->renderText($prompt),
+                    $hasDescription ? $this->renderText($prompt) : '',
                     info: 'Ctrl+D to submit'
                 )
                 ->when(
@@ -53,6 +61,21 @@
                     fn () => $this->newLine() // Space for errors
                 )
         };
+    }
+
+    /**
+     * Calculate the description width based on textarea content.
+     */
+    protected function calculateDescriptionWidth(Prompt $prompt, int $maxWidth): int
+    {
+        if (! $prompt instanceof TextareaPrompt) {
+            return $this->minWidth;
+        }
+
+        $titleWidth = mb_strwidth($this->stripEscapeSequences($prompt->label));
+        $textareaWidth = min($prompt->width, $maxWidth);
+
+        return max($this->minWidth, max($titleWidth, $textareaWidth));
     }
 
     /**
diff -ruN a/src/helpers.php b/src/helpers.php
--- a/src/helpers.php
+++ b/src/helpers.php
@@ -17,6 +17,7 @@
         mixed $validate = null,
         string $hint = '',
         ?Closure $transform = null,
+        ?string $description = null,
     ): string {
         return (new TextPrompt(...get_defined_vars()))->prompt();
     }
@@ -37,6 +38,7 @@
         mixed $validate = null,
         string $hint = '',
         ?Closure $transform = null,
+        ?string $description = null,
     ): string {
         return (new AutoCompletePrompt(...get_defined_vars()))->prompt();
     }
@@ -46,7 +48,7 @@
     /**
      * Prompt the user for number input.
      */
-    function number(string $label, string $placeholder = '', string $default = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?int $min = null, ?int $max = null, ?int $step = null): int|string
+    function number(string $label, string $placeholder = '', string $default = '', bool|string $required = false, mixed $validate = null, string $hint = '', ?int $min = null, ?int $max = null, ?int $step = null, ?string $description = null): int|string
     {
         return (new NumberPrompt(...get_defined_vars()))->prompt();
     }
@@ -65,6 +67,7 @@
         string $hint = '',
         int $rows = 5,
         ?Closure $transform = null,
+        ?string $description = null,
     ): string {
         return (new TextareaPrompt(...get_defined_vars()))->prompt();
     }
@@ -81,6 +84,7 @@
         mixed $validate = null,
         string $hint = '',
         ?Closure $transform = null,
+        ?string $description = null,
     ): string {
         return (new PasswordPrompt(...get_defined_vars()))->prompt();
     }
@@ -103,6 +107,7 @@
         bool|string $required = true,
         ?Closure $transform = null,
         string|Closure $info = '',
+        ?string $description = null,
     ): int|string {
         return (new SelectPrompt(...get_defined_vars()))->prompt();
     }
@@ -126,6 +131,7 @@
         string $hint = 'Use the space bar to select options.',
         ?Closure $transform = null,
         string|Closure $info = '',
+        ?string $description = null,
     ): array {
         return (new MultiSelectPrompt(...get_defined_vars()))->prompt();
     }
@@ -144,6 +150,7 @@
         mixed $validate = null,
         string $hint = '',
         ?Closure $transform = null,
+        ?string $description = null,
     ): bool {
         return (new ConfirmPrompt(...get_defined_vars()))->prompt();
     }
@@ -186,6 +193,7 @@
         string $hint = '',
         ?Closure $transform = null,
         string|Closure $info = '',
+        ?string $description = null,
     ): string {
         return (new SuggestPrompt(...get_defined_vars()))->prompt();
     }
@@ -208,6 +216,7 @@
         bool|string $required = true,
         ?Closure $transform = null,
         string|Closure $info = '',
+        ?string $description = null,
     ): int|string {
         return (new SearchPrompt(...get_defined_vars()))->prompt();
     }
@@ -230,6 +239,7 @@
         string $hint = 'Use the space bar to select options.',
         ?Closure $transform = null,
         string|Closure $info = '',
+        ?string $description = null,
     ): array {
         return (new MultiSearchPrompt(...get_defined_vars()))->prompt();
     }
