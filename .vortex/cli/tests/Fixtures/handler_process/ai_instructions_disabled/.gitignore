@@ -34,12 +34,6 @@
 recipes/*
 !recipes/page
 
-# Claude writes machine-local state into '.claude/', so it stays an allow
-# list: un-ignore only the files shared with the team.
-.claude/*
-!.claude/settings.json
-!.claude/skills/
-
 # Dependencies.
 /vendor
 /node_modules
