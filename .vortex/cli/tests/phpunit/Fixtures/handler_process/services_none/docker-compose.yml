@@ -52,8 +52,6 @@
   # Drupal Shield credentials.
   DRUPAL_SHIELD_USER: ${DRUPAL_SHIELD_USER:-}
   DRUPAL_SHIELD_PASS: ${DRUPAL_SHIELD_PASS:-}
-  # Redis integration flag.
-  DRUPAL_REDIS_ENABLED: ${DRUPAL_REDIS_ENABLED:-}
 
 # The default user under which the containers should run.
 x-user: &default-user
@@ -151,31 +149,6 @@
     ports:
       - '3306' # Database port in a container. Find port on host with `ahoy info` or `docker compose port database 3306`.
 
-  cache:
-    image: uselagoon/valkey-8:__VERSION__
-
-  search:
-    build:
-      context: .
-      dockerfile: .docker/solr.dockerfile
-    environment:
-      <<: *default-environment
-    volumes:
-      - search:/var/solr
-    ports:
-      - '8983' # Solr port in a container. Find port on host with `ahoy info` or `docker compose port search 8983`.
-
-  antivirus:
-    build:
-      context: .
-      dockerfile: .docker/clamav.dockerfile
-    environment:
-      <<: *default-environment
-    ports:
-      - '3310' # Find port on host with `docker compose port antivirus 3310`.
-    networks:
-      - default
-
   # Browser container, used for browser testing.
   browser:
     image: selenium/standalone-chromium:__VERSION__
@@ -192,16 +165,9 @@
   wait-for-dependencies:
     image: drevops/docker-wait-for-dependencies:__VERSION__
     depends_on:
-      - antivirus
       - database
-      - cache
-      - search
     command:
-      - antivirus:3310
       - database:3306
-      - cache:6379
-      - search:8983
-      - curl -s "http://search:8983/solr/drupal/select?q=*:*&rows=0&wt=json" | grep response
 
 networks:           ### Use external networks locally. Automatically removed in CI.
   amazeeio-network: ### Automatically removed in CI.
@@ -210,7 +176,6 @@
 volumes:
   app: {}
   files: {}
-  search: {}
 
 secrets:
   package_token:
