@@ -156,17 +156,6 @@
   cache:
     image: uselagoon/valkey-8:__VERSION__
 
-  search:
-    build:
-      context: .
-      dockerfile: .docker/solr.dockerfile
-    environment:
-      <<: *default-environment
-    volumes:
-      - search:/var/solr
-    ports:
-      - '8983' # Solr port in a container. Find port on host with `ahoy info` or `docker compose port search 8983`.
-
   antivirus:
     build:
       context: .
@@ -197,13 +186,10 @@
       - antivirus
       - database
       - cache
-      - search
     command:
       - antivirus:3310
       - database:3306
       - cache:6379
-      - search:8983
-      - curl -s "http://search:8983/solr/drupal/select?q=*:*&rows=0&wt=json" | grep response
 
 networks:           ### Use external networks locally. Automatically removed in CI.
   amazeeio-network: ### Automatically removed in CI.
@@ -212,7 +198,6 @@
 volumes:
   app: {}
   files: {}
-  search: {}
 
 secrets:
   package_token:
