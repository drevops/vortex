---
title: Vortex prose rules
---

# Prose rules

Vortex appears without emphasis.

Set VORTEX_DEBUG to see more output.

Edit the composer.json file.

This step is easy to follow.

The build does not run.

The build runs — and then stops.

The build may potentially fail.

For details, [click here](https://www.vortextemplate.com).

A powerful build pipeline.

The build has two stages.

The build runs linting, tests and deployments.

This sentence keeps going and going with clause after clause after clause so that it sails far past the word threshold that the rule enforces for every page in this documentation.

Please note that the build runs twice.

The build is very fast.

Run the build prior to the deployment.

See the table above.

The code is hosted on github.

There are many options.

Use a provider, e.g. Lagoon.

The build will run.

## Deployment Notifications

Text under the heading.

A phrase split across a wrapped line is still reported when it does
not use a contraction.

A list split across a wrapped line is still reported when it covers names or
identifiers, hashes or
addresses.

The build should not run twice.

The script runs in the `local`, `ci`, `dev` and `stage` environments and
skips production.

The runner ships Swift, PowerShell, .NET and CodeQL in its image.

The linter, the formatter and the test runner are installed together.

Pass an option, e.g.: a filter.
