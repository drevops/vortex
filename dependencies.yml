version: 2
dependencies:
- type: php
  path: /
  settings:
    composer_options: "--no-progress --no-suggest"
    github_labels:
    - dependencies
  manifest_updates:
    filters:
    - name: ".*"
      versions: "L.Y.Y"

- type: js
  path: /
  settings:
    github_labels:
    - dependencies
  manifest_updates:
    filters:
    - name: ".*"
      versions: "Y.Y.Y"
